source "$XDG_CONFIG_HOME/neomutt/creds_work"

set spoolfile = "$HOME/Mail/work/inbox"
set postponed = "$HOME/Mail/work/misc/drafts"
set record = ""
set from = "Alan - 1201 <mlk@1201.com>"
set signature = "$XDG_CONFIG_HOME/neomutt/sig_work"

color status red default
